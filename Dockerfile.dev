FROM python:3.12-slim
WORKDIR /app
COPY pyproject.toml .
COPY src/ src/
RUN pip install --no-cache-dir -e ".[test,lint]"
EXPOSE 8050
CMD ["uvicorn", "crmd_platform.server.app:create_app", "--factory", "--host", "0.0.0.0", "--port", "8050", "--reload"]
